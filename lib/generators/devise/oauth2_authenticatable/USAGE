Description:
    Generates a OAuth2 config file for your application. All the parameters are optional.

Example:
    rails g devise:oauth2_authenticatable APP_ID SECRET 'user,public_repo,repo,gist'

    This will create:
        config/oauth2_config.yml
